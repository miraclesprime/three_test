# Particle Wave Hero

Minimal Vite + React demo showing a neon particle wave using `three` and `@react-three/fiber`.

Quick start:

```bash
npm install
npm run dev
```

The `ParticleWave` component lives in `src/components/ParticleWave.jsx`.
